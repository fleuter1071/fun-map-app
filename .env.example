# Local development uses SQLite automatically when NODE_ENV=development.
NODE_ENV=development

# Supabase Postgres connection string (required when NODE_ENV is not development)
DATABASE_URL=postgresql://postgres:<PASSWORD>@<PROJECT-REF>.supabase.co:5432/postgres?sslmode=require

# Optional local port override
PORT=8010
